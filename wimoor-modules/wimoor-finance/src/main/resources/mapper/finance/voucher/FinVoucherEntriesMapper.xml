<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wimoor.finance.voucher.mapper.FinVoucherEntriesMapper">

    <resultMap type="com.wimoor.finance.voucher.domain.FinVoucherEntries" id="FinVoucherEntriesResult">
        <result property="entryId"    column="entry_id"    />
        <result property="groupid"    column="groupid"    />
        <result property="voucherId"    column="voucher_id"    />
        <result property="entryNo"    column="entry_no"    />
        <result property="subjectId"    column="subject_id"    />
        <result property="debitAmount"    column="debit_amount"    />
        <result property="creditAmount"    column="credit_amount"    />
        <result property="summary"    column="summary"    />
        <result property="createdTime"    column="created_time"    />
        <!-- 添加缺失的字段映射 -->
        <result property="currency"    column="currency"    />
        <result property="originalAmount"    column="original_amount"    />
        <result property="exchangeRate"    column="exchange_rate"    />
        <result property="quantity"    column="quantity"    />
        <result property="unitPrice"    column="unit_price"    />
    </resultMap>

    <sql id="selectFinVoucherEntriesVo">
        select entry_id, groupid, voucher_id, entry_no, subject_id, debit_amount, credit_amount, summary, created_time,
        currency, original_amount, exchange_rate, quantity, unit_price from fin_voucher_entries
    </sql>

    <select id="selectFinVoucherEntriesList" parameterType="com.wimoor.finance.voucher.domain.FinVoucherEntries" resultMap="FinVoucherEntriesResult">
        <include refid="selectFinVoucherEntriesVo"/>
        <where>
            <if test="groupid != null "> and groupid = #{groupid}</if>
            <if test="voucherId != null "> and voucher_id = #{voucherId}</if>
            <if test="entryNo != null "> and entry_no = #{entryNo}</if>
            <if test="subjectId != null  and subjectId != ''"> and subject_id = #{subjectId}</if>
            <if test="debitAmount != null "> and debit_amount = #{debitAmount}</if>
            <if test="creditAmount != null "> and credit_amount = #{creditAmount}</if>
            <if test="summary != null  and summary != ''"> and summary = #{summary}</if>
            <if test="createdTime != null "> and created_time = #{createdTime}</if>
            <!-- 添加缺失的字段条件 -->
            <if test="currency != null  and currency != ''"> and currency = #{currency}</if>
            <if test="originalAmount != null "> and original_amount = #{originalAmount}</if>
            <if test="exchangeRate != null "> and exchange_rate = #{exchangeRate}</if>
            <if test="quantity != null "> and quantity = #{quantity}</if>
            <if test="unitPrice != null "> and unit_price = #{unitPrice}</if>
        </where>
    </select>

    <resultMap type="com.wimoor.finance.voucher.domain.FinVoucherEntries" id="FinVoucherEntriesWithNameResult">
        <result property="entryId"    column="entry_id"    />
        <result property="groupid"    column="groupid"    />
        <result property="groupName"    column="group_name"    />
        <result property="voucherId"    column="voucher_id"    />
        <result property="entryNo"    column="entry_no"    />
        <result property="subjectId"    column="subject_id"    />
        <result property="subjectName"    column="subject_name"    />
        <result property="debitAmount"    column="debit_amount"    />
        <result property="creditAmount"    column="credit_amount"    />
        <result property="summary"    column="summary"    />
        <result property="createdTime"    column="created_time"    />
        <result property="currency"    column="currency"    />
        <result property="originalAmount"    column="original_amount"    />
        <result property="exchangeRate"    column="exchange_rate"    />
        <result property="quantity"    column="quantity"    />
        <result property="unitPrice"    column="unit_price"    />
    </resultMap>

    <select id="selectFinVoucherEntriesListWithJoin" parameterType="com.wimoor.finance.voucher.domain.dto.FinVoucherDTO" resultType="java.util.Map">
        SELECT e.*,v.voucher_type,v.voucher_no,v.voucher_date,v.attachment_count,v.voucher_status,v.remark,s.subject_name,s.subject_code,
        v.auditor_by,v.preparer_by
        FROM fin_voucher_entries e
        LEFT JOIN fin_vouchers v ON e.voucher_id=v.voucher_id
        LEFT JOIN fin_accounting_subjects s ON s.subject_id=e.subject_id AND s.groupid=e.groupid
        <where>
            and v.groupid = #{groupid}
            <if test="startDate != null "> and v.voucher_date &gt;= #{startDate}</if>
            <if test="endDate != null "> and v.voucher_date &lt;= #{endDate}</if>
            <if test="voucherId != null "> and v.voucher_id = #{voucherId}</if>
            <if test="voucherNo != null "> and v.voucher_no = #{voucherNo}</if>
            <if test="entryNo != null "> and o.entry_no = #{entryNo}</if>
            <if test="subjectId != null  and subjectId != ''">
                and exists(
                select 1 from  fin_voucher_entries e
                where e.voucher_id = v.voucher_id
                and e.groupid = v.groupid
                and e.subject_id = #{subjectId}
                )
            </if>
            <if test="summary != null  and summary != ''">
                and exists(
                select 1 from  fin_voucher_entries e
                where e.voucher_id = v.voucher_id
                and e.groupid = v.groupid
                and e.summary = #{summary}
                )
            </if>
            <if test="auditorBy != null "> and v.auditor_by = #{auditorBy}</if>
            <if test="preparerBy != null "> and v.preparer_by = #{preparerBy}</if>
            <if test="createdTime != null "> and v.created_time = #{createdTime}</if>
        </where>
        order by v.voucher_date desc,v.voucher_no desc
    </select>

    <select id="selectFinVoucherEntriesByEntryId" parameterType="Long" resultMap="FinVoucherEntriesResult">
        <include refid="selectFinVoucherEntriesVo"/>
        where entry_id = #{entryId}
    </select>

    <insert id="insertFinVoucherEntries" parameterType="com.wimoor.finance.voucher.domain.FinVoucherEntries" useGeneratedKeys="true" keyProperty="entryId">
        insert into fin_voucher_entries
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="groupid != null">groupid,</if>
            <if test="voucherId != null">voucher_id,</if>
            <if test="entryNo != null">entry_no,</if>
            <if test="subjectId != null">subject_id,</if>
            <if test="debitAmount != null">debit_amount,</if>
            <if test="creditAmount != null">credit_amount,</if>
            <if test="summary != null">summary,</if>
            <if test="createdTime != null">created_time,</if>
            <!-- 添加缺失的字段插入 -->
            <if test="currency != null">currency,</if>
            <if test="originalAmount != null">original_amount,</if>
            <if test="exchangeRate != null">exchange_rate,</if>
            <if test="quantity != null">quantity,</if>
            <if test="unitPrice != null">unit_price,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="groupid != null">#{groupid},</if>
            <if test="voucherId != null">#{voucherId},</if>
            <if test="entryNo != null">#{entryNo},</if>
            <if test="subjectId != null">#{subjectId},</if>
            <if test="debitAmount != null">#{debitAmount},</if>
            <if test="creditAmount != null">#{creditAmount},</if>
            <if test="summary != null">#{summary},</if>
            <if test="createdTime != null">#{createdTime},</if>
            <!-- 添加缺失的字段值 -->
            <if test="currency != null">#{currency},</if>
            <if test="originalAmount != null">#{originalAmount},</if>
            <if test="exchangeRate != null">#{exchangeRate},</if>
            <if test="quantity != null">#{quantity},</if>
            <if test="unitPrice != null">#{unitPrice},</if>
        </trim>
    </insert>

    <update id="updateFinVoucherEntries" parameterType="com.wimoor.finance.voucher.domain.FinVoucherEntries">
        update fin_voucher_entries
        <trim prefix="SET" suffixOverrides=",">
            <if test="groupid != null">groupid = #{groupid},</if>
            <if test="voucherId != null">voucher_id = #{voucherId},</if>
            <if test="entryNo != null">entry_no = #{entryNo},</if>
            <if test="subjectId != null">subject_id = #{subjectId},</if>
            <if test="debitAmount != null">debit_amount = #{debitAmount},</if>
            <if test="creditAmount != null">credit_amount = #{creditAmount},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="createdTime != null">created_time = #{createdTime},</if>
            <!-- 添加缺失的字段更新 -->
            <if test="currency != null">currency = #{currency},</if>
            <if test="originalAmount != null">original_amount = #{originalAmount},</if>
            <if test="exchangeRate != null">exchange_rate = #{exchangeRate},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="unitPrice != null">unit_price = #{unitPrice},</if>
        </trim>
        where entry_id = #{entryId}
    </update>

    <delete id="deleteFinVoucherEntriesByEntryId" parameterType="Long">
        delete from fin_voucher_entries where entry_id = #{entryId}
    </delete>

    <delete id="deleteFinVoucherEntriesByEntryIds" parameterType="String">
        delete from fin_voucher_entries where entry_id in
        <foreach item="entryId" collection="array" open="(" separator="," close=")">
            #{entryId}
        </foreach>
    </delete>
     <delete id="deleteFinVoucherEntriesByVoucherId" parameterType="com.wimoor.finance.voucher.domain.FinVouchers">
        delete from fin_voucher_entries where voucher_id = #{voucherId} and groupid = #{groupid}
    </delete>
    <delete id="deleteByVoucherId" parameterType="Long">
        delete from fin_voucher_entries where voucher_id = #{voucherId}
    </delete>

    <select id="selectByVoucherId" parameterType="Long" resultMap="FinVoucherEntriesResult">
        <include refid="selectFinVoucherEntriesVo"/>
        where voucher_id = #{voucherId}
    </select>

    <!-- 批量查询分录（按凭证ID列表），用于消除 N+1 查询 -->
    <select id="selectByVoucherIds" resultMap="FinVoucherEntriesWithNameResult">
        select e.entry_id,
               e.groupid,
               e.voucher_id,
               e.entry_no,
               e.subject_id,
               e.debit_amount,
               e.credit_amount,
               e.summary,
               e.created_time,
               s.subject_name as subject_name,
               e.currency,
               e.exchange_rate,
               e.quantity,
               e.unit_price,
               e.original_amount
        from fin_voucher_entries e
        left join fin_accounting_subjects s on e.subject_id = s.subject_id and e.groupid = s.groupid
        where e.voucher_id in
        <foreach item="vid" collection="list" open="(" separator="," close=")">
            #{vid}
        </foreach>
        order by e.voucher_id, e.entry_no
    </select>
</mapper>