# 仓库-调库单模块功能解析文档

## 1. 模块架构

### 1.1 前端架构

调库单模块采用Vue 3 + Element Plus开发，实现了模块化、响应式的调库单管理界面。模块位于`wimoor-ui/src/views/erp/warehouse/transfer`目录下，主要包含以下文件：

| 文件路径 | 功能描述 |
|---------|--------|
| `index.vue` | 主调库单管理组件，包含调库单列表和状态筛选 |
| `components/header.vue` | 头部组件，实现调库单的搜索和导入功能 |
| `components/create.vue` | 创建组件，实现调库单的创建和编辑功能 |
| `components/details.vue` | 详情组件，实现调库单的详细信息展示和状态流转操作 |

### 1.2 后端架构

后端采用Spring Boot框架实现，提供RESTful API接口。相关代码位于`wimoor-erp`模块中，主要包含以下部分：

| 模块 | 功能描述 |
|---------|--------|
| `DispatchWarehouseFormController.java` | 调库单管理的RESTful API接口 |
| `DispatchFormServiceImpl.java` | 调库单管理的业务逻辑实现 |
| `IDispatchFormEntryService.java` | 调库单明细的业务逻辑接口 |
| `DispatchForm.java` | 调库单实体类 |
| `DispatchFormEntry.java` | 调库单明细实体类 |
| `DispatchFormRecord.java` | 调库单操作记录实体类 |

## 2. 核心功能实现

### 2.1 调库单管理

#### 2.1.1 调库单列表

**前端实现**：
- 在`index.vue`中，通过`loadTableData`函数加载调库单列表
- 使用Element Plus的`el-table`组件展示调库单数据
- 支持按状态筛选和关键词搜索

**核心代码**：
```javascript
function loadTableData(params){
    if(params.auditstatus=='1' || params.auditstatus=='2' || params.auditstatus=='3'){
        params.fromDate=undefined;
        params.toDate=undefined;
    }
    dispatchApi.list(params).then((res)=>{
        state.tableData.records = res.data.records;
        state.tableData.total =res.data.total;
    })
}
```

**后端实现**：
- 在`DispatchWarehouseFormController.java`中，通过`getListData`方法处理调库单列表请求
- 使用MyBatis-Plus的分页查询功能
- 支持按关键词搜索和状态筛选

**核心代码**：
```java
@PostMapping("/list")
public Result<IPage<Map<String, Object>>> getListData(@RequestBody DispatchWarehouseFormDTO dto){
    Map<String, Object> map = new HashMap<String, Object>();
    UserInfo user = UserInfoContext.get();
    String shopid = user.getCompanyid();
    String search = dto.getSearch();
    String auditstatus =dto.getAuditstatus();
    if (StrUtil.isEmpty(search)) {
        search = null;
    } else {
        search = search.trim() + "%";
    }
    List<Integer> statusList = new ArrayList<Integer>();
    if (StrUtil.isEmpty(auditstatus)) {
        statusList = null;
    } else if (auditstatus.equals("weichuli")) {
        statusList.add(0);
        statusList.add(1);
    } else if (auditstatus.equals("yichuli")) {
        statusList.add(2);
        statusList.add(3);
    } else {
        statusList.add(Integer.parseInt(auditstatus));
    }
    String fromDate = dto.getFromDate();
    SimpleDateFormat sdf = new SimpleDateFormat("yyyy-MM-dd");
    if (StrUtil.isNotEmpty(fromDate)) {
        map.put("fromDate", fromDate.trim());
    } else {
        Calendar cal = Calendar.getInstance();
        cal.add(Calendar.DAY_OF_MONTH, -30);
        fromDate = GeneralUtil.formatDate(cal.getTime(), sdf);
        map.put("fromDate", fromDate);
    }
    String toDate = dto.getToDate();
    if (StrUtil.isNotEmpty(toDate)) {
        map.put("endDate", toDate.trim());
    } else {
        toDate = GeneralUtil.formatDate(new Date(), sdf);
        map.put("endDate", toDate);
    }
    map.put("shopid", shopid);
    map.put("search", search);
    map.put("statusList", statusList);
    IPage<Map<String, Object>> list = dispatchFormService.findByCondition(dto.getPage(),map);
    return  Result.success(list);
}
```

#### 2.1.2 创建调库单

**前端实现**：
- 在`create.vue`中，通过`submitForm`函数处理调库单提交
- 使用Element Plus的`el-form`组件收集调库单信息
- 通过`handleAdd`函数添加调库商品

**核心代码**：
```javascript
function submitForm(ftype){
    if(state.form.fromwarehouseid==state.form.towarehouseid){
        ElMessage.error( "不能选择相同仓库！ ");
    }else{
        var isok=getSKUList();
        if(isok==true){
            var data={};
            data.action=ftype;
            data.skumapstr=JSON.stringify(state.skulist);
            data.remark=state.form.remark;
            data.fromwarehouseid=state.form.fromwarehouseid;
            data.towarehouseid=state.form.towarehouseid;
            data.arrivaltime=state.form.arrivaltime;
            data.ftype=state.form.ftype;
            data.id="";
            dispatchApi.saveData(data).then((res)=>{
                isok=true;
                if(res.data){
                    ElMessage.success( "添加成功！");
                    if(consformid!=null && consformid!=undefined && consformid!=''){
                         preprocessApi.update({"formid":consformid,"donetype":"dispatch"});
                    }
                    if(res.data && res.data.id && res.data.id!=null && res.data.id!="" && res.data.id!=undefined){
                            router.push({
                                path:"/e/w/t/d",
                                query:{
                                    title:'调库单详情',
                                    path:"/e/w/t/d",
                                    id:res.data.id,
                                    replace:true
                                },
                            })
                    }
                }else{
                    ElMessage.error(res.data);
                }
            });
        }
    }
}
```

**后端实现**：
- 在`DispatchWarehouseFormController.java`中，通过`addDataAction`方法处理调库单创建请求
- 生成调库单编号并保存调库单信息
- 调用`saveAction`方法保存调库单明细

**核心代码**：
```java
@Transactional
@PostMapping("/saveData")
public Result<Map<String, Object>> addDataAction(@RequestBody DispatchWarehouseItemDTO dto) {
    UserInfo user = UserInfoContext.get();
    DispatchForm dispatchForm = new DispatchForm();
    String shopid =user.getCompanyid();
    dispatchForm.setShopid(shopid);
    String id = dto.getId();
    String action = dto.getAction();
    if (action.equals("save")) {
        dispatchForm.setAuditstatus(0);//0：未提交，1：提交未审核，2：已审核 ，3：已驳回
    } else if(action.equals("passdone")) {
        dispatchForm.setAuditstatus(4);
    } else {
        dispatchForm.setAuditstatus(1);
    }
    if (StrUtil.isNotEmpty(id)) {
        dispatchForm.setId(id);
    } else {
        try {
            dispatchForm.setNumber(serialNumService.readSerialNumber(shopid, "PD"));
        } catch (Exception e) {
            e.printStackTrace();
            try {
                dispatchForm.setNumber(serialNumService.readSerialNumber(shopid, "PD"));
            } catch (Exception e1) {
                e1.printStackTrace();
                throw new BizException("编码获取失败,请联系管理员");
            }
        }
        dispatchForm.setAuditor(user.getId());
        dispatchForm.setCreator(user.getId());
        dispatchForm.setCreatedate(new Date());
        dispatchForm.setId(warehouseService.getUUID());
    }
    dispatchForm.setOperator(user.getId());
    String fromwarehouseid = dto.getFromwarehouseid();
    String towarehouseid = dto.getTowarehouseid();
    String arrivalTime = dto.getArrivaltime();
    String remark = dto.getRemark();
    dispatchForm.setFromWarehouseid(fromwarehouseid);
    dispatchForm.setToWarehouseid(towarehouseid);
    dispatchForm.setRemark(remark);
    dispatchForm.setFtype(dto.getFtype());
    dispatchForm.setAudittime(new Date());
    dispatchForm.setOpttime(new Date());
    if(!StrUtil.isEmpty(arrivalTime)) {
        dispatchForm.setArrivalTime(GeneralUtil.getDatez(arrivalTime));
    }
    String skumapstr = dto.getSkumapstr();// {"b1662cd7-b3c1-4e93-9389-44e704f9f542":"5","04585a54-364e-4066-b790-ad6a70e1cf34":"10"}
    if (StrUtil.isEmpty(skumapstr)) {
        throw new BizException("SKU调库列表不能为空");
    }
    Map<String, Object> map = dispatchFormService.saveAction(user,dispatchForm, skumapstr);
    return Result.success(map);
}
```

#### 2.1.3 调库单导入

**前端实现**：
- 在`header.vue`中，通过`uploadExcel`函数处理Excel文件上传
- 使用Element Plus的`el-upload`组件实现文件上传
- 支持下载导入模板

**核心代码**：
```javascript
function uploadExcel(){
    let FormDatas = new FormData();
    FormDatas.append('file',state.myfile);
    dispatchApi.uploadExcel(FormDatas).then(function(res){
        if(res.data!="上传成功"){
            ElMessage.error(res.data);
        }else{
            ElMessage.success('上传成功');
            state.uploadVisible = false;
            handleQuery();
        }
    })
}
function downloadTemp(){
    dispatchApi.downExcelTemp();
}
```

**后端实现**：
- 在`DispatchWarehouseFormController.java`中，通过`uploadExcelAction`方法处理Excel文件上传
- 解析Excel文件内容并创建调库单

**核心代码**：
```java
@PostMapping(value = "/uploadExcel",consumes = MediaType.MULTIPART_FORM_DATA_VALUE)
public Result<String> uploadExcelAction(@RequestParam("file")MultipartFile file){
    UserInfo user = UserInfoContext.get();
    if (file != null) {
        try {
            InputStream inputStream = file.getInputStream();
            Workbook workbook = WorkbookFactory.create(inputStream);
            Sheet sheet = workbook.getSheetAt(0);
            String result = dispatchFormService.uploadDispatchStockByExcel(sheet, user);
            workbook.close();
            return Result.success(result);
        } catch (IOException e) {
            e.printStackTrace();
            throw new BizException("文件读写错误");
        } catch (EncryptedDocumentException e) {
            e.printStackTrace();
            throw new BizException("文件解码错误");
        }
    }
    return Result.failed();
}
```

### 2.2 调库流程管理

#### 2.2.1 调库单审核

**前端实现**：
- 在`details.vue`中，通过`reSubmitForm`函数处理调库单审核
- 显示审核确认对话框
- 调用`examine`接口提交审核结果

**核心代码**：
```javascript
function reSubmitForm(status,ftype){
    //状态的回滚和提交操作
    var titleStr="";
    var title="";
    if(ftype=="back"){
        if(status==1){
            titleStr="您确认要驳回该调库单吗？";
            title="驳回调库单";
            status=5;
        }else if(status==2){
            titleStr="您确认要取消该调库单吗？";
            title="取消调库单";
            status=5;
        }else if(status==3){
            titleStr="您确认要取消该调库单吗？";
            title="取消调库单";
            status=5;
        }else if(status==4){
            titleStr="您确认要撤销该调库单吗？";
            title="撤销调库单";
            status=5;
        } 
    }
    if(ftype=="submit"){
        if(status==0){
            titleStr="您确认要提交该调库单吗？";
            title="提交调库单";
            status=1;
        }else if(status==1){
            titleStr="您确认要通过该调库单吗？";
            title="通过调库单";
            status=2;
        }else if(status==2){
            titleStr="您确认要发货出库吗？";
            title="发货调库单";
            status=3;
        }else if(status==3){
            titleStr="请确认您已收到所有货物？";
            title="收货调库单";
            status=4;
        } 
    }
    if(ftype=="done"){
        titleStr="您确定要直接完成发货单吗？";
        title="完成调库单";
        status=4;
    }
     ElMessageBox.confirm(
        titleStr,
        title,
        {
          confirmButtonText: '确定',
          cancelButtonText: '取消',
          type: 'warning',
        }
      )
        .then(() => {
                dispatchApi.examine({"ids":id,"status":status}).then((res)=>{
                      if(res.data){
                          ElMessage.success(res.data);
                          load();
                      }else{
                          ElMessage.error("操作失败");
                      }
                  });
        })
        .catch(() => {
          ElMessage.info('取消操作');
        })
}
```

**后端实现**：
- 在`DispatchFormServiceImpl.java`中，通过`submitAction`方法处理调库单状态变更
- 根据状态变更执行相应的库存操作

**核心代码**：
```java
@Transactional
public int submitAction( UserInfo user,String ids,String status ) throws BizException {
    int result = 0;
    String[] idlist = ids.split(",");
    for (int i = 0; i < idlist.length; i++) {
        String id = idlist[i];
        if (GeneralUtil.isNotEmpty(id)) {
            DispatchForm dispatchForm = getById(id);
            if ("1".equals(status)||"0".equals(status)) {
                //提交后不可编辑
            }
            if ("2".equals(status)) {
                if(dispatchForm.getAuditstatus()==1) {
                   changeInventory(user,dispatchForm);
                }
            }else if("3".equals(status)) {
                if(dispatchForm.getAuditstatus()==1) {
                    changeInventory(user,dispatchForm);
                    outBoundOut(user,dispatchForm);
                }
                if(dispatchForm.getAuditstatus()==2) {
                    outBoundOut(user,dispatchForm);
                }
            }else if("4".equals(status)) {
                if(dispatchForm.getAuditstatus()==1||dispatchForm.getAuditstatus()==0) {
                    changeInventory(user,dispatchForm);
                    outBoundOut(user,dispatchForm);
                    inBoundToFulfillable(user,dispatchForm);
                }
                if(dispatchForm.getAuditstatus()==2) {
                    outBoundOut(user,dispatchForm);
                    inBoundToFulfillable(user,dispatchForm);
                }
                if(dispatchForm.getAuditstatus()==3) {
                    inBoundToFulfillable(user,dispatchForm);
                }
            }else if("5".equals(status)) {
                if(dispatchForm.getAuditstatus()==2) {
                    cancelInventory(user,dispatchForm);
                }
                if(dispatchForm.getAuditstatus()==3) {
                    cancelOut(user,dispatchForm);
                }
                if(dispatchForm.getAuditstatus()==4) {
                    cancelIn(user,dispatchForm);
                }
                if(dispatchForm.getAuditstatus()==0) {
                    this.baseMapper.deleteById(dispatchForm);
                }
            }
            if("0".equals(status)) {
                dispatchForm.setAudittime(new Date());
                dispatchForm.setAuditstatus(Integer.parseInt(status));
                result = this.baseMapper.updateById(dispatchForm);
            }
            dispatchFormRecordMapper.insert(new DispatchFormRecord(dispatchForm));
        }
    }
    return result;
}
```

#### 2.2.2 库存操作

**后端实现**：
- 在`DispatchFormServiceImpl.java`中，通过`changeInventory`、`outBoundOut`、`inBoundToFulfillable`等方法处理库存变更
- 调用`inventoryFormAgentService`执行具体的库存操作

**核心代码**：
```java
public void changeInventory(UserInfo user,DispatchForm dispatchForm) {
    List<DispatchFormEntry> entrylist = dispatchFormEntryService.findByFormid(dispatchForm.getId());
    InventoryParameter parameter_out = new InventoryParameter();
    parameter_out.setShopid(dispatchForm.getShopid());
    parameter_out.setFormid(dispatchForm.getId());
    parameter_out.setNumber(dispatchForm.getNumber());
    parameter_out.setFormtype("dispatch");
    parameter_out.setWarehouse(dispatchForm.getFromWarehouseid());
    parameter_out.setOperator(dispatchForm.getOperator());
    parameter_out.setStatus(EnumByInventory.Ready);
    
    InventoryParameter parameter_in = new InventoryParameter();
    parameter_in.setShopid(dispatchForm.getShopid());
    parameter_in.setFormid(dispatchForm.getId());
    parameter_in.setFormtype("dispatch");
    parameter_in.setWarehouse(dispatchForm.getToWarehouseid());
    parameter_in.setOperator(dispatchForm.getOperator());
    parameter_in.setNumber(dispatchForm.getNumber());
    parameter_in.setStatus(EnumByInventory.Ready);
    
    for(int i=0;i<entrylist.size();i++) {
        DispatchFormEntry entry = entrylist.get(i);
        String materialid = entry.getMaterialid();
        parameter_out.setMaterial(materialid);
        parameter_out.setAmount(entry.getAmount());
        inventoryFormAgentService.outStockByRead(parameter_out);
        parameter_in.setMaterial(materialid);
        parameter_in.setAmount(entry.getAmount());
        inventoryFormAgentService.inStockByRead(parameter_in);
    }
}

public void outBoundOut(UserInfo user ,DispatchForm dispatchForm) {
    InventoryParameter parameter_out = new InventoryParameter();
    parameter_out.setShopid(dispatchForm.getShopid());
    parameter_out.setFormid(dispatchForm.getId());
    parameter_out.setNumber(dispatchForm.getNumber());
    parameter_out.setFormtype("dispatch");
    parameter_out.setWarehouse(dispatchForm.getFromWarehouseid());
    parameter_out.setOperator(dispatchForm.getOperator());
    parameter_out.setStatus(EnumByInventory.alReady);
    List<Map<String, Object>> entrylist = dispatchFormEntryService.selectByFormid(dispatchForm.getId());
    if (entrylist != null && entrylist.size() > 0) {
        for (int j = 0; j < entrylist.size(); j++) {
            parameter_out.setMaterial(entrylist.get(j).get("materialid").toString());
            parameter_out.setAmount(Integer.parseInt(entrylist.get(j).get("amount").toString()));
            inventoryFormAgentService.outStockByRead(parameter_out);
        }
    }
}

public void inBoundToFulfillable(UserInfo user ,DispatchForm dispatchForm){
    InventoryParameter parameter_in = new InventoryParameter();
    parameter_in.setShopid(dispatchForm.getShopid());
    parameter_in.setFormid(dispatchForm.getId());
    parameter_in.setFormtype("dispatch");
    parameter_in.setWarehouse(dispatchForm.getToWarehouseid());
    parameter_in.setOperator(dispatchForm.getOperator());
    parameter_in.setNumber(dispatchForm.getNumber());
    parameter_in.setStatus(EnumByInventory.alReady);
    List<Map<String, Object>> entrylist = dispatchFormEntryService.selectByFormid(dispatchForm.getId());
    if (entrylist != null && entrylist.size() > 0) {
        for (int j = 0; j < entrylist.size(); j++) {
            parameter_in.setMaterial(entrylist.get(j).get("materialid").toString());
            parameter_in.setAmount(Integer.parseInt(entrylist.get(j).get("amount").toString()));
            inventoryFormAgentService.inStockByRead(parameter_in);
        }
    }
}
```

## 3. API接口设计

### 3.1 调库单管理

| API路径 | 方法 | 功能描述 | 请求参数 | 成功返回 |
|---------|------|---------|---------|----------|
| `/erp/api/v1/inventory/dispatchform/list` | POST | 获取调库单列表 | search, auditstatus, fromDate, toDate | 调库单列表及总数 |
| `/erp/api/v1/inventory/dispatchform/getData` | GET | 获取调库单详情 | id | 调库单详情及明细 |
| `/erp/api/v1/inventory/dispatchform/saveData` | POST | 保存调库单 | action, skumapstr, fromwarehouseid, towarehouseid, arrivaltime, ftype | 保存结果 |
| `/erp/api/v1/inventory/dispatchform/examine` | GET | 审核调库单 | ids, status | 审核结果 |
| `/erp/api/v1/inventory/dispatchform/deleteData` | GET | 删除调库单 | ids | 删除结果 |
| `/erp/api/v1/inventory/dispatchform/downExcelTemp` | GET | 下载导入模板 | 无 | Excel模板文件 |
| `/erp/api/v1/inventory/dispatchform/uploadExcel` | POST | 导入调库单 | file | 导入结果 |

## 4. 技术要点

### 4.1 前端技术

1. **Vue 3 Composition API**：使用`ref`、`reactive`、`toRefs`等组合式API管理状态
2. **Element Plus**：使用表格、表单、对话框等组件构建界面
3. **组件通信**：使用`emit`和`ref`实现组件间通信
4. **响应式设计**：通过Vue的响应式系统实现数据与视图的双向绑定
5. **表单验证**：使用Element Plus的表单验证功能确保数据有效性
6. **文件上传**：实现Excel文件的上传和模板下载功能

### 4.2 后端技术

1. **Spring Boot**：构建RESTful API接口
2. **MyBatis-Plus**：简化数据库操作，提供分页查询功能
3. **事务管理**：使用`@Transactional`注解确保调库操作的原子性
4. **Excel处理**：使用Apache POI处理Excel文件的导入和导出
5. **库存操作**：实现库存的调整和回滚功能
6. **操作记录**：记录调库单的所有操作历史

## 5. 安全考虑

1. **权限控制**：基于用户角色的权限控制，确保只有授权用户可以执行相应操作
2. **库存校验**：在调库前检查调出仓库的库存是否充足
3. **数据验证**：前端和后端双重数据验证，确保数据有效性
4. **事务管理**：使用事务确保调库操作的原子性，避免数据不一致
5. **操作记录**：记录所有调库操作，便于审计和追溯

## 6. 性能优化

1. **前端优化**：
   - 使用Vue 3的Composition API减少模板代码
   - 合理使用组件拆分，提高代码复用性
   - 表格数据分页加载，提高大数据量场景下的性能

2. **后端优化**：
   - 使用MyBatis-Plus的分页查询，减少数据库压力
   - 批量处理调库单明细，减少数据库操作次数
   - 合理使用事务，避免长事务影响系统性能

## 7. 扩展建议

1. **功能扩展**：
   - 添加调库单审批流程配置，支持自定义审批流程
   - 增加调库单的批量操作功能，如批量审核、批量发货等
   - 添加调库单的统计报表，便于分析调库情况
   - 支持调库单的打印功能，生成调库单纸质单据

2. **技术扩展**：
   - 前端使用TypeScript增强类型安全性
   - 后端使用Redis缓存提升库存查询性能
   - 添加API文档（如Swagger）方便开发人员使用
   - 实现调库单的国际化支持

3. **用户体验**：
   - 添加调库单的状态变更通知，及时通知相关人员
   - 优化调库单导入功能，支持更多格式的Excel文件
   - 添加调库单的批量导入预览功能，减少导入错误
   - 实现调库单的条码/二维码生成，方便仓库操作

## 8. 总结

调库单模块是Wimoor系统中实现仓库间商品调拨的核心模块，通过Vue 3 + Element Plus和Spring Boot技术栈，实现了完整的调库流程管理功能。模块设计合理，功能完善，安全可靠，为用户提供了便捷的库存调拨工具。

通过本解析文档，开发人员可以了解调库单模块的实现原理和技术细节，为后续的功能扩展和维护提供参考。同时，用户也可以通过帮助文档快速掌握调库单的操作方法，提高库存管理的效率和准确性。