# 记事本模块功能解析文档

## 1. 模块架构

### 1.1 前端架构

**技术栈：**
- Vue 3 + Composition API
- Element Plus UI组件库
- TinyMCE富文本编辑器
- Axios网络请求

**核心文件结构：**
```
wimoor-ui/src/views/sys/notepad/
├── index.vue          # 主页面组件
└── content_dialog.vue # 内容对话框组件

wimoor-ui/src/api/sys/tool/
└── notepadApi.js      # API调用接口
```

**前端组件结构：**
- **主页面组件** (`index.vue`)：包含笔记列表、搜索功能和富文本编辑器
- **内容对话框组件** (`content_dialog.vue`)：用于显示笔记内容详情
- **MaterialDialog组件**：复用的产品选择对话框，用于SKU绑定

### 1.2 后端架构

**技术栈：**
- Spring Boot
- MyBatis-Plus ORM框架
- RESTful API设计
- 自定义权限控制

**核心文件结构：**
```
wimoor-admin/admin-boot/src/main/java/com/wimoor/sys/tool/
├── controller/
│   └── SysNotepadController.java  # 控制器
├── service/
│   ├── INotepadService.java       # 服务接口
│   └── impl/
│       └── NotepadServiceImpl.java # 服务实现
├── mapper/
│   └── NotepadMapper.java         # 数据访问接口
├── pojo/
│   ├── entity/
│   │   └── Notepad.java           # 实体类
│   └── dto/
│       └── SysNotepadQueryDTO.java # 查询DTO
└── ...
```

**数据库表结构：**
- **t_sys_tool_notepad**：存储笔记数据的主表

## 2. 核心功能实现

### 2.1 前端核心功能实现

#### 2.1.1 笔记列表与搜索

**实现代码：**
```javascript
// 搜索功能实现
function handleQuery(type){
    notepadApi.list(state.queryParams).then(res=>{
            state.tableData.records=res.data.records;
            state.tableData.total=res.data.total;
            if(type=='init'&&state.tableData&&state.tableData.records&&state.tableData.records.length>0){
                nextTick(()=>{
                    state.notepad=JSON.parse(JSON.stringify(state.tableData.records[0]));
                    mytableRef.value.setCurrentRow(state.tableData.records[0])
                })
            }
    })
}
```

**技术要点：**
- 使用Element Plus的表格组件展示笔记列表
- 支持按标题、内容、SKU三种类型进行搜索
- 分页加载，提高性能
- 初始化时自动加载第一条笔记内容

#### 2.1.2 富文本编辑器集成

**实现代码：**
```javascript
const editorConfig = ref({
  height: 'calc(100vh - 113px)',
  language: 'zh_CN',
  language_url: '/tinymce/langs/zh_CN.js',
  menubar: 'file edit view insert format tools',
  plugins: [
    'advlist', 'autolink', 'lists', 'link', 'image', 'charmap', 'preview', 'anchor',
    'searchreplace', 'visualblocks', 'code','codesample', 'fullscreen',
    'insertdatetime', 'media', 'table', 'help', 'wordcount', 'quickbars'
  ],
  toolbar:[
  'undo redo',
  'blocks',
  'bold italic underline strikethrough',
  'forecolor backcolor',
  'removeformat',
  'alignleft aligncenter alignright alignjustify',
  'bullist numlist outdent indent',
  'link quickimage table  ',
  'codesample help'
  ].join(' | '),
  quickbars_selection_toolbar: 'bold italic | quicklink h2 h3 blockquote ',
  quickbars_insert_toolbar: 'quickimage quicktable',
  table_toolbar: 'tableprops tabledelete | tableinsertrowbefore tableinsertrowafter tabledeleterow | tableinsertcolbefore tableinsertcolafter tabledeletecol'
})
```

**技术要点：**
- 集成TinyMCE富文本编辑器
- 自定义工具栏配置，满足编辑需求
- 响应式高度设置，适配不同屏幕尺寸
- 支持中文语言包

#### 2.1.3 笔记保存与更新

**实现代码：**
```javascript
function handleSave(){
    state.saveloading=true;
    notepadApi.save(state.notepad).then(res=>{
        ElMessage.success("保存成功");
        state.saveloading=false;
        handleQuery();
    }).catch(e=>{
        state.saveloading=false;
    })
}
```

**技术要点：**
- 异步保存，避免阻塞界面
- 保存后自动刷新笔记列表
- 错误处理，确保操作稳定性
- 加载状态显示，提升用户体验

#### 2.1.4 SKU绑定功能

**实现代码：**
```javascript
function getRows(rows) { 
    let materialid = "";
    if(rows && rows.length>0){
        rows.forEach(function(item){
                if(materialid.indexOf((item.id+","))<0){
                     materialid+=item.id+",";
                }
        });
        var data = { mark: state.bindNotepad.id, materialid: materialid, ftype: "notepad" };
        markApi.notepad(data).then(res=>{
                ElMessage.success("绑定成功");
        })
    }
}

function handleBindSku(row) {
    state.bindNotepad=row;
    MaterialRef.value.show( );
}
```

**技术要点：**
- 复用产品选择对话框
- 批量选择产品进行绑定
- 调用专门的markApi进行绑定操作
- 操作结果反馈

### 2.2 后端核心功能实现

#### 2.2.1 控制器实现

**实现代码：**
```java
@ApiOperation("查询记事本内容")
@PostMapping("/list")
public Result<?> queryNotepad(@RequestBody SysNotepadQueryDTO queryDTO) {
    UserInfo userInfo = UserInfoContext.get();
    queryDTO.setShopid(userInfo.getCompanyid());
    // 可以在这里添加权限验证逻辑，确保用户只能查询自己有权限的店铺数据
    return Result.success(iNotepadService.queryNotepad(queryDTO));
}

@ApiOperation("查询记事本内容")
@PostMapping("/save")
public Result<?> saveNotepad(@RequestBody Notepad notepad) {
    UserInfo userInfo = UserInfoContext.get();
    // 可以在这里添加权限验证逻辑，确保用户只能查询自己有权限的店铺数据
    notepad.setShopid(userInfo.getCompanyid());
    notepad.setOperator(userInfo.getId());
    return Result.success(iNotepadService.saveNotepad(notepad));
}
```

**技术要点：**
- 使用`UserInfoContext`获取当前用户信息
- 自动注入shopid和operator信息
- 权限控制，确保数据隔离
- RESTful API设计风格

#### 2.2.2 服务层实现

**实现代码：**
```java
@Override
public IPage<Notepad> queryNotepad(SysNotepadQueryDTO queryDTO) {
    // 1. 创建分页对象 (页码从1开始，默认每页10条)
    IPage<Notepad> page = new Page<>(queryDTO.getCurrentpage(), queryDTO.getPagesize());

    // 2. 构建查询条件
    QueryWrapper<Notepad> queryWrapper = new QueryWrapper<>();

    // 必传条件：shopid
    queryWrapper.eq("shopid", queryDTO.getShopid());
     if(StrUtil.isNotBlank(queryDTO.getSearch())) {
        if("title".equals(queryDTO.getSearchtype())) {
            queryWrapper.like("title", "%"+queryDTO.getSearch().trim()+"%");
        }else if("content".equals(queryDTO.getSearchtype())) {
            queryWrapper.like("content", "%"+queryDTO.getSearch().trim()+"%");
        }else if("sku".equals(queryDTO.getSearchtype())) {
            queryWrapper.like("sku", "%"+queryDTO.getSearch().trim()+"%");
        }
    }

    // 按创建时间倒序 (最新的在前)
    queryWrapper.orderByDesc("opttime");

    // 3. 执行分页查询
    return baseMapper.selectPage(page, queryWrapper);
}

@Override
public Object saveNotepad(Notepad notepad) {
    if (notepad.getId() == null) {
        notepad.setOpttime(new Date());
        Notepad old = this.lambdaQuery().eq(Notepad::getShopid, notepad.getShopid()).eq(Notepad::getTitle, notepad.getTitle()).one();
        if (old != null) {
            throw new BizException("标题已存在");
        }
        return baseMapper.insert(notepad);
    }else {
        notepad.setOpttime(new Date());
        return baseMapper.updateById(notepad);
    }
}
```

**技术要点：**
- 使用MyBatis-Plus的分页和查询功能
- 动态构建查询条件，支持多维度搜索
- 标题重复检查，确保数据唯一性
- 自动更新操作时间
- 区分新增和更新操作

## 3. API接口设计

### 3.1 前端API调用

| API方法 | 接口路径 | 请求参数 | 响应数据 | 描述 |
|---------|---------|---------|---------|------|
| `list` | `/admin/api/v1/notepad/list` | `{currentpage, pagesize, searchtype, search}` | 分页数据对象 | 查询笔记列表 |
| `save` | `/admin/api/v1/notepad/save` | `{id, title, content, sku}` | 操作结果 | 保存笔记 |
| `removeNote` | `/admin/api/v1/notepad/removeNote` | `{id}` | 操作结果 | 删除笔记 |
| `view` | `/admin/api/v1/notepad/view` | `{id}` | 笔记对象 | 查看单个笔记 |
| `notepad` | `/api/v1/material/mark/notepad` | `{mark, materialid, ftype}` | 操作结果 | 绑定SKU |

### 3.2 后端API接口

| 接口路径 | 请求方法 | 功能描述 | 请求体 (JSON) | 响应体 (JSON) |
|---------|---------|---------|--------------|--------------|
| `/api/v1/notepad/list` | POST | 查询笔记列表 | `{"currentpage":1, "pagesize":20, "searchtype":"title", "search":"关键词"}` | `{"code":0, "msg":"success", "data":{"records":[], "total":0, ...}}` |
| `/api/v1/notepad/view` | GET | 查看单个笔记 | N/A (参数通过URL传递) | `{"code":0, "msg":"success", "data":{"id":"1", "title":"...", ...}}` |
| `/api/v1/notepad/save` | POST | 保存笔记 | `{"title":"新笔记", "content":"<p>内容</p>", "sku":""}` | `{"code":0, "msg":"success", "data":1}` |
| `/api/v1/notepad/removeNote` | POST | 删除笔记 | `{"id":"1"}` | `{"code":0, "msg":"success", "data":true}` |

## 4. 数据模型

### 4.1 前端数据模型

**状态管理：**
```javascript
const state = reactive({
  notepad:{ content: '<p></p>', title:""},
  queryParams:{currentpage:1,pagesize:20,searchtype:"title",search:''},
  tableData:{records:[],total:0},
  saveloading: false,
  bindNotepad: {},
});
```

**核心数据结构：**
- **notepad**：当前编辑的笔记对象
- **queryParams**：搜索和分页参数
- **tableData**：笔记列表数据
- **bindNotepad**：当前正在绑定SKU的笔记

### 4.2 后端数据模型

**实体类：**
```java
@TableName(value ="t_sys_tool_notepad")
@Data
public class Notepad {
    private String id;
    private String shopid;
    private String title;
    private String sku;
    private String content;
    private String operator;
    private Date opttime;
}
```

**查询DTO：**
```java
@Data
public class SysNotepadQueryDTO {
    private Integer currentpage;
    private Integer pagesize;
    private String searchtype;
    private String search;
    private String shopid;
}
```

**数据库表结构：**
| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| `id` | `bigint unsigned` | `NOT NULL` | 笔记ID |
| `shopid` | `bigint unsigned` | `NOT NULL` | 店铺ID |
| `title` | `varchar(500)` | `NOT NULL DEFAULT ''` | 笔记标题 |
| `sku` | `char(50)` | `NOT NULL DEFAULT ''` | 绑定的SKU |
| `content` | `varchar(2000)` | `NOT NULL DEFAULT ''` | 笔记内容 |
| `operator` | `bigint unsigned` | `DEFAULT NULL` | 操作人ID |
| `opttime` | `datetime` | `DEFAULT NULL` | 操作时间 |

## 5. 技术亮点与创新点

### 5.1 前端技术亮点

1. **富文本编辑器集成**：成功集成TinyMCE编辑器，提供丰富的文本编辑功能
2. **响应式设计**：使用计算高度和弹性布局，适配不同屏幕尺寸
3. **组件复用**：复用MaterialDialog组件，减少代码冗余
4. **性能优化**：
   - 使用`nextTick`确保DOM更新后再操作
   - 编辑器内容同步机制，避免重复渲染
   - 页面可见性监听，优化编辑器性能
5. **用户体验**：
   - 实时保存状态反馈
   - 编辑器初始化和刷新机制
   - 平滑的内容切换效果

### 5.2 后端技术亮点

1. **权限控制**：基于UserInfoContext的权限管理，确保数据安全
2. **数据验证**：标题重复检查，确保数据一致性
3. **查询优化**：
   - 索引字段使用（shopid、opttime）
   - 动态SQL构建，减少不必要的查询
   - 分页查询，提高大数据量场景性能
4. **RESTful API设计**：规范的API路径和请求/响应格式
5. **错误处理**：使用自定义BizException处理业务错误

## 6. 安全考虑

### 6.1 前端安全

1. **输入验证**：富文本编辑器内容的安全过滤，防止XSS攻击
2. **权限控制**：前端路由权限控制，确保只有授权用户可访问
3. **数据传输**：使用HTTPS协议传输数据，加密敏感信息
4. **错误处理**：避免在前端暴露详细错误信息

### 6.2 后端安全

1. **权限验证**：基于UserInfoContext的权限控制，确保用户只能访问自己店铺的数据
2. **输入验证**：后端对所有输入参数进行验证，防止恶意输入
3. **SQL注入防护**：使用MyBatis-Plus的参数化查询，防止SQL注入
4. **异常处理**：统一的异常处理机制，避免敏感信息泄露
5. **数据隔离**：通过shopid字段确保数据按店铺隔离

## 7. 性能优化

### 7.1 前端性能优化

1. **编辑器性能**：
   - 延迟加载编辑器实例
   - 页面可见性监听，优化编辑器资源使用
   - 内容同步机制，减少不必要的DOM操作

2. **数据加载**：
   - 分页加载笔记列表
   - 按需获取笔记内容
   - 搜索条件优化，减少无效查询

3. **渲染优化**：
   - 使用Vue 3的Composition API，减少组件实例开销
   - 合理使用v-if和v-show，优化条件渲染
   - 避免不必要的计算属性和监听器

### 7.2 后端性能优化

1. **数据库优化**：
   - 合理的索引设计（shopid、opttime等字段）
   - 使用MyBatis-Plus的缓存机制
   - 批量操作优化

2. **查询优化**：
   - 动态SQL构建，减少不必要的字段查询
   - 分页查询，避免一次性加载大量数据
   - 合理使用like查询，避免全表扫描

3. **代码优化**：
   - 减少数据库查询次数
   - 合理使用缓存
   - 异步处理非关键操作

## 8. 总结与建议

### 8.1 模块优势

1. **功能完整**：提供了笔记管理的全流程功能
2. **用户友好**：富文本编辑器和直观的操作界面
3. **安全可靠**：完善的权限控制和数据验证
4. **性能良好**：优化的查询和渲染机制
5. **扩展性强**：模块化设计，便于后续功能扩展

### 8.2 改进建议

1. **功能扩展**：
   - 添加笔记分类功能
   - 支持笔记标签管理
   - 实现笔记版本历史
   - 添加团队协作编辑功能

2. **性能优化**：
   - 实现前端本地缓存，减少重复请求
   - 后端添加Redis缓存，提高查询性能
   - 优化富文本编辑器的加载速度

3. **用户体验**：
   - 添加笔记模板功能
   - 实现笔记导出功能（PDF、Markdown等）
   - 添加快捷键支持
   - 优化移动端适配

4. **安全增强**：
   - 实现笔记访问权限控制
   - 添加操作审计日志
   - 增强输入内容的安全过滤

5. **集成建议**：
   - 与其他模块（如产品管理、订单管理）的深度集成
   - 支持从其他模块直接创建相关笔记
   - 实现笔记与系统其他实体的关联管理

### 8.3 未来发展方向

记事本模块作为公司内部工具，具有很大的发展潜力。未来可以考虑：

1. **知识管理平台**：扩展为公司内部的知识管理平台，集成文档管理、知识库等功能
2. **AI辅助**：集成AI功能，如智能摘要、内容推荐等
3. **移动应用**：开发独立的移动应用，方便用户随时随地访问和编辑笔记
4. **协作功能**：增强团队协作功能，支持多人同时编辑和评论
5. **数据分析**：添加笔记使用数据分析，提供知识管理洞察

通过持续优化和功能扩展，记事本模块可以成为公司内部知识管理的核心工具，为提高工作效率和知识共享做出更大贡献。

---

**技术文档版本：** 1.0.0
**更新时间：** 2026-02-03