spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      master:
        url: jdbc:mysql://${mysql.host}:${mysql.port}/db_admin?serverTimezone=UTC&useUnicode=true@characterEncoding=utf-8
        username: ${mysql.username}
        password: ${mysql.password}
        driver-class-name: com.mysql.cj.jdbc.Driver
      slave:
        url: jdbc:mysql://127.0.0.1:3306/db_datamove?serverTimezone=UTC&useUnicode=true@characterEncoding=utf-8
        username: root
        password: 123456
        driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
      multipart:
            max-file-size: 10MB
            max-request-size: 100MB
  cloud:
    loadbalancer:
      ribbon:
        enabled: false
      nacos:
        enabled: true
mybatis-plus:
  global-config:
      db-config:
         update-strategy: IGNORED
feign:
  client:
    config:
      default:
        connectTimeout: 10000
        readTimeout: 600000
